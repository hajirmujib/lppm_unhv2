import 'package:flutter/material.dart';

extension MyColor on Color {
  /// String is in the format "aabbcc" or "ffaabbcc" with an optional leading "#".
  static const Color primaryColor = Color(0xff614291);
  static const Color secondaryColor = Color(0xff907AB3);
  static const Color thirdaryColor = Color(0xffc7a4ff);
}
